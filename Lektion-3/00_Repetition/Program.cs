/* 
    LOOPS
    ----------------------------------------------------------------
    while(condition) {}
    do {} while (condition); 
    for (int i=0; condition; i++) {}
    foreach (var item in items) {}

    STATEMENTS
    ----------------------------------------------------------------
    if (condition(s)) {}
    if (condition(s)) {} else {}
    if (condition(s)) {} else if (condition(s)) {} else {}

    switch(value) { case value: break;}
    switch(value) { case value: break; default: break; }
    
    CLASSES (class, object, model, entity)
    ----------------------------------------------------------------
    Är en mall som representerar ett objekt av något slag

    class User 
    {
        fields

        Properties { get; set; }

        Construtor(s)

        Method(s)
    }
*/